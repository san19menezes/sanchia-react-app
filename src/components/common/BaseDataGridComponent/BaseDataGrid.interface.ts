// Library imports
import { GridColDef } from "@mui/x-data-grid";

// Define the Typescript interface for Reusable Data Grid
export interface BaseDataGridInterface {
  rows: any;
  columns: GridColDef[];
}
